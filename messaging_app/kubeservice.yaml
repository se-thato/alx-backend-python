apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  sele00000000ctor:
    app: messaging-app-green  # switch between 'messaging-app' (blue) and 'messaging-app-green'
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
